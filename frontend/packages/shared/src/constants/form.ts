// Uppercase eyebrow above a form field — shared by the dialog Selects across
// features (invite/reassign DW, assign device, create project, add DW) so the
// label style stays identical instead of drifting per copy.
export const FIELD_LABEL_CLASS =
  "text-foreground-secondary text-xs font-semibold tracking-wider uppercase";
