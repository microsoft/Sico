/**
 * Re-export of `@sico/shared/testing/network` (the canonical location).
 * Single definition under `@sico/shared/src/testing/network.ts`.
 */
export {
  fireNetworkStatus,
  restoreOnline,
  setOnline,
} from "@sico/shared/testing/network.ts";
