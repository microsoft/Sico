/**
 * Playwright global setup placeholder.
 *
 * Phase 3 of the scaffold plan uses Playwright's per-test `page.route()`
 * API to intercept the backend (rather than reusing the unit-test `msw`
 * setup). When/if cross-cutting setup is needed (a shared logged-in
 * fixture, a process-wide cookie, etc.), wire it through this file via
 * `globalSetup` in `playwright.config.ts`.
 */
export {};
