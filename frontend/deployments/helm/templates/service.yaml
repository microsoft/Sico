apiVersion: v1
kind: Service
metadata:
  name: sico-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: sico-frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
