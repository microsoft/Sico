# Mark TanStack Router auto-generated files so GitHub diff views collapse
# them and language stats don't count them.
#
# CQ-N1: `routeTree.gen.ts` is regenerated by `@tanstack/router-plugin/vite`
# on every dev/build run; the file is checked in so downstream consumers
# don't have to run the plugin to typecheck. Hand-edits will be silently
# overwritten on the next dev start.
packages/app/src/routeTree.gen.ts linguist-generated=true
